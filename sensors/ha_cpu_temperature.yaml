# Measure the raspberry pi CPU temperature.

platform: command_line
name: HA CPU temperature
command: ~/.homeassistant/bin/sensor.ha_cpu_temperature
value_template: "{{ value | float }}"
unit_of_measurement: "°C"
