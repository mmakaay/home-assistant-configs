# A templated sensor, used to extract the temperature from an AQARA motion sensor.

platform: template
sensors:
  hal_temperatuur:
    value_template: "{{ state_attr('binary_sensor.hal_beweging', 'temperature') }}"
    unit_of_measurement: "°C"
