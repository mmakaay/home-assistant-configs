# A templated sensor, used to extract the temperature from an AQARA door sensor.

platform: template
sensors:
  tuindeuren_temperatuur:
    value_template: "{{ state_attr('binary_sensor.tuindeuren_open', 'temperature') }}"
    unit_of_measurement: "°C"
