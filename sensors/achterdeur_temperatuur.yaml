# A templated sensor, used to extract the temperature from an AQARA door sensor.

platform: template
sensors:
  achterdeur_temperatuur:
    value_template: "{{ state_attr('binary_sensor.achterdeur_open', 'temperature') }}"
    unit_of_measurement: "°C"
