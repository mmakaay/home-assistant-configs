# A templated sensor, used to extract the temperature from an AQARA door sensor.

platform: template
sensors:
  toiletdeur_temperatuur:
    value_template: "{{ state_attr('binary_sensor.toiletdeur_open', 'temperature') }}"
    unit_of_measurement: "°C"
